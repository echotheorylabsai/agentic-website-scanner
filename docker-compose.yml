services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: local
      POSTGRES_DB: agentic
    ports: ["5434:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
